# Многоязычная система и новые страницы

## ✅ Реализовано

### 1. Система переключения языков (KZ/RU)

**Созданные файлы:**
- `frontend/locales/translations.ts` - Полные переводы на казахский и русский языки
- `frontend/stores/language.ts` - Pinia store для управления языком

**Функционал:**
- Переключение между казахским (KZ) и русским (RU) языками
- Сохранение выбранного языка в localStorage
- Автоматическая загрузка сохраненного языка при входе
- Переключатель в header на всех страницах
- Полный перевод всех интерфейсных элементов

**Использование в коде:**
```vue
<script setup>
import { useLanguageStore } from '~/stores/language'

const langStore = useLanguageStore()
const t = computed(() => langStore.t)
</script>

<template>
  <h1>{{ t.dashboard.title }}</h1>
  <button @click="langStore.setLanguage('kz')">KZ</button>
  <button @click="langStore.setLanguage('ru')">RU</button>
</template>
```

### 2. Новые страницы

#### 📋 Страница Tickets (`/tickets`)
**Функционал:**
- Таблица всех заявок с полной информацией
- Фильтрация по статусу (Новые/В работе/Решенные)
- Фильтрация по приоритету (Высокий/Средний/Низкий)
- Поиск по названию и описанию
- Цветовые индикаторы статусов и приоритетов
- Пагинация
- Переход к детальной информации по заявке
- Кнопка создания новой заявки

**Данные:**
- ID заявки
- Описание
- Приоритет (с цветными badges)
- Статус (с цветными badges)
- Исполнитель
- Срок выполнения
- Ссылка на детали

#### 📊 Страница Reports (`/reports`)
**Функционал:**
- Выбор периода отчета (дата начала - дата окончания)
- Экспорт в PDF и Excel (UI готов)
- Статистика по инцидентам:
  - Общее количество инцидентов
  - Среднее время ответа
  - Среднее время решения
  - Процент решенных

**Аналитика:**
- График инцидентов по статусам (прогресс-бары)
- График инцидентов по приоритетам (прогресс-бары)
- Топ-5 частых проблем (таблица с трендами)
- Продуктивность команды (карточки сотрудников)

#### 👤 Страница Profile (`/profile`)
**Функционал:**
- **Личная информация:**
  - Полное имя
  - Email
  - Телефон
  - Должность
  - Отдел
  - Аватар с инициалами

- **Настройки аккаунта:**
  - Выбор языка (Русский/Қазақша/English)
  - Часовой пояс
  - Email уведомления (toggle)

- **Безопасность:**
  - Смена пароля
  - Двухфакторная аутентификация (UI готов)

- **Метаданные:**
  - Последний вход
  - Дата создания аккаунта

### 3. Обновленная навигация

**Боковое меню (sidebar):**
- ✅ Dashboard (/)
- ✅ Tickets (/tickets) - **НОВОЕ**
- ✅ Projects (/ProjectsCreate)
- ✅ Reports (/reports) - **НОВОЕ**
- ✅ Status (/status)
- ✅ Profile (/profile) - **НОВОЕ**

Все пункты меню теперь переведены и реагируют на смену языка.

## 🌐 Структура переводов

### Секции в translations.ts:
- `common` - общие элементы (кнопки, статусы, пагинация)
- `auth` - страница входа
- `dashboard` - главная панель
- `tickets` - управление заявками
- `ticketDetail` - детали заявки
- `projects` - создание проектов
- `reports` - отчеты и аналитика
- `profile` - профиль пользователя
- `status` - статус системы
- `footer` - подвал

## 🎨 Дизайн

Все новые страницы следуют корпоративному стилю АРРФР:
- Цветовая схема: Blue-600 (основной), Slate-900 (sidebar), Gray-50 (фон)
- Шрифт: Inter
- Tailwind CSS для всех стилей
- Адаптивная верстка (responsive design)
- Единый Layout с sidebar и header

## 🚀 Запуск

Система уже запущена в Docker:
```bash
# Пересобрать и запустить
docker compose build frontend-portal
docker compose up -d

# Проверить логи
docker logs widget-frontend-portal-1 --tail=30
```

**Доступ:**
- Frontend: http://localhost:3001
- Backend API: http://localhost:8001
- Логин: `root` / Пароль: `123`

## 📝 Следующие шаги

Если потребуется:
1. Добавить больше языков (английский уже упоминается в profile)
2. Подключить реальное API для tickets и reports
3. Реализовать функционал экспорта PDF/Excel
4. Добавить реальную смену пароля через backend
5. Интегрировать 2FA
